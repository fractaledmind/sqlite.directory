<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "sqlite.directory" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
  </head>

  <body>
    <header class="text-lg max-w-6xl mx-auto py-4 flex justify-between items-center border-b">
      <h2 class="flex items-center gap-2">
        <%= image_tag "/icon.svg" %>
        <%= link_to root_path, class: "group" do %>
          <code>
            <span class="text-blue-500 group-hover:underline decoration-blue-500">sqlite</span>.<span class="text-black group-hover:underline decoration-black">directory</span>
          </code>
        <% end %>
      </h2>

      <div class="inline-flex items-center gap-2">
        <%= button_to "/developer/authorization?origin=sign_up", data: { turbo: false }, class: "bg-blue-500 text-white rounded-md px-3 py-1 flex items-center space-x-2 hover:ring-4 hover:ring-blue-200" do %>
          <%= render_svg "github" %>
          <span class="font-semibold">Sign in</span>
        <% end %>
      </div>
    </header>

    <main class="max-w-6xl mx-auto text-center h-full pt-24">
      <%= yield %>
    </main>

    <div class="fixed top-0 left-0 right-0 text-center py-2">
      <% if notice.present? %>
        <p id="notice"
           class="py-2 px-3 bg-green-50 text-green-500 font-medium rounded-lg inline-block"
           data-controller="fade">
          <%= notice %>
        </p>
      <% end %>

      <% if alert.present? %>
        <p id="alert"
           class="py-2 px-3 bg-red-50 text-red-500 font-medium rounded-lg inline-block"
           data-controller="fade">
          <%= alert %>
        </p>
      <% end %>
    </div>
  </body>
</html>
